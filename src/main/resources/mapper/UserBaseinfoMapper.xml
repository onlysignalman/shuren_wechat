<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.shuren.mapper.resume.UserBaseinfoMapper">
    <insert id="register" parameterType="UserBaseinfo" useGeneratedKeys="true" keyProperty="userId">
		INSERT INTO user_baseinfo
			(openId, nckName, mobile, email, password, 
			createTimeStamp, lastUpdateTimeStamp, isDel)
	    values
	    	(#{openId}, #{nckName}, #{mobile}, #{email}, #{password}, 
	    	unix_timestamp(now()), unix_timestamp(now()), 0)
	</insert>
	
	<select id="login" parameterType="Map" resultType="UserBaseinfo">
		SELECT 
			* 
		FROM 
			user_baseinfo 
		WHERE 
			mobile = #{mobile} AND password = #{password}
	</select>
	
	<select id="queryCount" parameterType="Map" resultType="Long">
		SELECT 
			COUNT(1) 
		FROM 
			user_baseinfo 
		WHERE 
			mobile = #{mobile} AND password = #{password}
	</select>
	
	<select id="queryList" parameterType="Map" resultType="Map">
		SELECT 
			* 
		FROM 
			user_baseinfo 
		WHERE 
			mobile = #{mobile} AND password = #{password}
	</select>
	
	<update id="forget" parameterType="UserBaseinfo">
		UPDATE 
			user_baseinfo 
		SET 
			password = #{password}, lastUpdateTimeStamp = unix_timestamp(now()) 
		WHERE 
			mobile = #{mobile}
	</update>
</mapper>